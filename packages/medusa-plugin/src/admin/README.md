# Admin extensions

The plugin has no admin extensions yet. `medusa plugin:build` always runs the
admin build step and fails when `src/admin` is missing, so this directory stays
(as in Medusa's plugin starter).
